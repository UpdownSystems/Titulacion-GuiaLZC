<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pymespace.guiazlzc">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" /> <!-- Used only for testing purposes, not required for Firebase Storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Maps -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_guiazlzc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".MapActivity" />
        <activity android:name=".UploadImageActivity" />
        <activity
            android:name=".TabsMisNegociosActivity"
            android:label="Mis negocios"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PostDetailScrollingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditPostActivity"
            android:label="Editar negocio" />
        <activity
            android:name=".ResetPasswordActivity"
            android:label="Restablecer contraseña" />
        <activity
            android:name=".MisPostsActivity"
            android:label="Mis negocios" />
        <activity android:name=".TodoPerfilActivity" />
        <activity
            android:name=".NewPostActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="Nuevo negocio" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2" />
        <activity
            android:name=".editPerfil"
            android:label="Editar perfil" />
        <activity android:name=".listaNegocios" />
        <activity
            android:name=".perfil"
            android:label="Mi Perfil" />
        <activity
            android:name=".navigationDrawer"
            android:label="@string/title_activity_navigation_drawer" />
        <activity android:name=".registroUsuario" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden|stateUnchanged">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MyDownloadService"
            android:exported="false" />
        <service
            android:name=".service.MyUploadService"
            android:exported="false" />
    </application>

</manifest>